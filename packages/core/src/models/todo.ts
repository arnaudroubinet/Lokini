/**
 * To-do document type (post-MVP).
 * Items with label, status (pending/in-progress/done), optional assignee.
 * CRDT map for properties, CRDT set for items, CRDT sequence for ordering.
 */

export {};
