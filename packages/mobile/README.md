# @lokini/mobile

Application React Native (iOS + Android) — **post-MVP**.

Ce package contiendra le client mobile Lokini utilisant React Native avec `@lokini/core` comme logique métier partagée.

## Stack prévue

- React Native
- @lokini/core (workspace dependency)
- react-i18next (internationalisation)
- SQLite (stockage local)
